#include "matrix.h"
#include <matplot/matplot.h>
#include <iostream>
#include <initializer_list>

int main()
{
    /*using namespace matplot;
    std::vector<double> theta = linspace(0,2*pi);
    std::vector<double> x1 = transform(theta, [](auto x) { return cos(x); });
    std::vector<double> y1 = transform(theta, [](auto x) { return sin(x); });

    Matrix<double> a{ 2, static_cast<int>(x1.size()), {x1,y1} };

    Matrix<double> t{ 2, 2, {
        9,1,
        1,6
    } };

    Matrix<double> r{ t * a };

    std::vector<double> m1 = linspace(-10, 5);
    std::vector<double> m2 = transform(m1, [](auto x) { return x; });

    

    plot(x1, y1, r.row(1), r.row(2));
    show();*/

    std::cout << "Finally done learning git" << '\n';

    
       




    
    




    
    

    

    

    

    







    return 0;
}

